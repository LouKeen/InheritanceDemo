//Here we have a class Dog which extends(inherits) the class Animal.
// This means that the class Dog would also have access to the methods of the class Animal.
public class Dog extends Animal {

    // Special method of class Dog.
    public void bark() {
        System.out.println("Arf!");
    }
}
